# Gradient Descent as Search — Facial Attribute Prediction

Linear and logistic regression implemented using gradient descent to predict facial attributes, with experiments analyzing learning rates, convergence, and loss behavior.

##  Start

- Install dependencies:

python -m pip install -r requirements.txt

Manually install dataset if error is encountered in data loading and preprocessing. This is a restriction of the google drive where the dataset is sourced.